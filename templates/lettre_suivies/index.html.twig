{% extends 'base.html.twig' %}

{% block title %}LettreSuivies index{% endblock %}

{% block body %}
    <h1>LettreSuivies index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lettre_suivy in lettre_suivies %}
            <tr>
                <td>{{ lettre_suivy.id }}</td>
                <td>{{ lettre_suivy.nom }}</td>
                <td>
                    <a href="{{ path('app_lettre_suivies_show', {'id': lettre_suivy.id}) }}">show</a>
                    <a href="{{ path('app_lettre_suivies_edit', {'id': lettre_suivy.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_lettre_suivies_new') }}">Create new</a>
{% endblock %}
