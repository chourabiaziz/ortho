{% extends 'xbase/base.html.twig' %}

{% block title %}nouveau LettreSuivies{% endblock %}

{% block body %}
<div class="container-xxl py-5">
    <div class="container">
<h1>Contunier votre lettre</h1>
  
 {{ form_start(form) }}

        <div class="row">
            <div class="col-12">
                <div class="form-row">
                     <div class="form-group ">
                        {{ form_label(form.ortho , 'orthophoniste' )}}
                        {{ form_widget(form.ortho, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <br>
                    <div class="form-group ">
                        {{ form_label(form.nomP , 'nom de patient' )}}
                        {{ form_widget(form.nomP, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.nomP) }}
                    </div>
                     
                    <br>
                    <button class="btn btn-success">{{ button_label|default('Continuer') }}</button>





                    
{{ form_end(form) }}                    </div>
                    </div>

{% endblock %}