{% extends 'base.html.twig' %}

{% block title %}LettreSuivies{% endblock %}

{% block body %}
    <h1>LettreSuivies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lettre_suivy.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lettre_suivy.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lettre_suivies_index') }}">retour à la liste</a>

    <a href="{{ path('app_lettre_suivies_edit', {'id': lettre_suivy.id}) }}">modifier</a>

    {{ include('lettre_suivies/_delete_form.html.twig') }}
{% endblock %}
