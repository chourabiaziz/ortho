<form method="post" action="{{ path('app_lettre_suivies_delete', {'id': lettre_suivy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ lettre_suivy.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
