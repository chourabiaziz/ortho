{% extends 'base.html.twig' %}

{% block title %}Profil index{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Emplacement</th>
                <th>Diplome</th>
                <th>Faculte</th>
                <th>Specialite</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for profil in profils %}
            <tr>
                <td>{{ profil.id }}</td>
                <td>{{ profil.emplacement }}</td>
                <td>{{ profil.diplome }}</td>
                <td>{{ profil.faculte }}</td>
                <td>{{ profil.specialite }}</td>
                <td>
                    <a href="{{ path('app_profil_show', {'id': profil.id}) }}">Consultet</a>
                    <a href="{{ path('app_profil_edit', {'id': profil.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_profil_new') }}">Créer un nouveau</a>
{% endblock %}
